# Backend API base URL (no trailing slash)
# For local dev with proxy: leave unset (defaults to /api/gst)
# For Vercel production: set to your deployed Spring Boot URL
VITE_API_BASE=https://your-backend-url.com/api/gst
